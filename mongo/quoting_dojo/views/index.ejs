<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>QuotingDojo - Home</title>
    </head>
    <body>
        <h2>Welcome to QuotingDojo</h2>
        <% if (typeof(errors) != 'undefined') { %>
            <ul>
            <% for (const x in errors) { %>
                <li><%= errors[x].message %></li>
            <% } %>
            </ul>
        <% } %>
        <form action="/quotes" method="post">
            Your Name: <input type="text" name="name"><br>
            Your Quote: <textarea name="content" cols="30" rows="5"></textarea><br>
            <input type="submit" value="Add my quote!">
        </form>
        <a href="/quotes"><button>Skip to quotes!</button></a>
    </body>
</html>
